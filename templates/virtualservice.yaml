apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: my-virtualservice
  namespace: myapp-ns
spec:
  hosts:
  - "*"
  gateways:
  - istio-ingressgateway.istio-system.svc.cluster.local
  http:
  - route:
    - destination:
        host: rollout-ngnix-active
        port:
          number: 80
      weight: 100
    - destination:
        host: rollout-ngnix-preview
        port:
          number: 80
      weight: 0
